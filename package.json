{
  "name": "botwhatsapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "clear-auth": "node clear-auth.js",
    "docker:build": "docker build -t botwhatsapp .",
    "docker:run": "docker run -p 3000:3000 -e DOCKER_ENV=true botwhatsapp",
    "docker:run-detached": "docker run -d -p 3000:3000 -e DOCKER_ENV=true --name botwhatsapp botwhatsapp",
    "docker:stop": "docker stop botwhatsapp && docker rm botwhatsapp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.3",
    "nodemailer": "^7.0.6",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.1"
  }
}
